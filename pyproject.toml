[project]
name = "python-fastapi-langchain-worker"
version = "0.1.0"
description = "A Cloudflare Python Worker with Workers AI"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "workers-py",
]
